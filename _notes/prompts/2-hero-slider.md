Take a look at unpacked project, i want you to make chnages for:
1. hero-slider-images
2. hero-slider-content

Instead of 2 widgets, there should be one only, i.e,:
- hero-slider

this single element should:
- ask user to set variables for each cell
- each cell having image gaget (as it is currently)
- and each image gaget currently also has title caption and link which is awesome

Changes:
- currently the title and image are different, instead it should be one single cell set via one single section
- the slider quickly shifts to next, making it difficult for user to control via drags or buttons, make it natural. you can use swiper library methods for that
- also for conetnts of each card, we need updates
- it should have a background image with other elements centeredd
- there shold be anoptional heading title, an optional para and a button
- user should be able to set variables for title, contents, its button lable (if not set then it is always learn more) and link for button
- make it responsive so that content is still set in cell on small devices
- I only want a samll heading like "John Doe" and 1 sentence for para and a button.
- so adjust accordingly on different sized screens.

First study the project.
Then make a plan with milestones then achive one by one.