can you make another script "xml_comparison.py` that takes 2 xmls paths and writes structural diffs in a diffs dir with timestamp as prefix

so that we can find actual differences in 2 xmls

there are already 2 xmls in project packed dir, use that and save results once to `diffs` dir in project.